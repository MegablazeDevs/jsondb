{
  "name": "jsondb",
  "version": "1.0.0",
  "description": "A quick and dirty way to use json as a db. All fs calls are handled by jsondb, simplifying usage.",
  "main": "./out/main.js",
  "scripts": {
    "compile": "tsc -p ./",
    "lint": "eslint src --ext ts",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "test": "node ./out/test/runTest.js"
  },
  "author": "MegaBlazeDevs",
  "license": "AGPLv3",
  "devDependencies": {
    "@types/node": "^13.11.0",
    "@typescript-eslint/eslint-plugin": "^2.34.0",
    "@typescript-eslint/parser": "^2.34.0",
    "eslint": "^6.8.0",
    "typescript": "^3.9.7"
  }
}
